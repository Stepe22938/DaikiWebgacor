openapi: 3.1.0
info:
  # Do not change the title, if the title changes, the import paths will be broken
  title: Api
  version: 0.1.0
  description: MC Roleplay Server API
servers:
  - url: /api
    description: Base API path
tags:
  - name: health
    description: Health operations
  - name: developments
    description: Server development items
  - name: announcements
    description: Server announcements
  - name: users
    description: User management
  - name: stats
    description: Server stats
  - name: follows
    description: Follow system
  - name: messaging
    description: Chat and messaging
  - name: settings
    description: User settings
  - name: credits
    description: Server credits system
  - name: badges
    description: Profile badge system
  - name: forms
    description: Forms and Voting system
  - name: gacha
    description: Gacha and Cosmetics system

paths:
  /healthz:
    get:
      operationId: healthCheck
      tags: [health]
      summary: Health check
      responses:
        "200":
          description: Healthy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthStatus"

  /gacha/board:
    get:
      operationId: getGachaBoard
      tags: [gacha]
      summary: Get gacha board metadata and user state
      responses:
        "200":
          description: Gacha board details
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GachaBoardResult"

  /gacha/claim-diamonds:
    post:
      operationId: claimDiamonds
      tags: [gacha]
      summary: Claim free daily test diamonds
      responses:
        "200":
          description: Claim success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GachaClaimResult"

  /gacha/spin:
    post:
      operationId: spinGacha
      tags: [gacha]
      summary: Spin the gacha wheel
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SpinGachaInput"
      responses:
        "200":
          description: Spin results
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SpinGachaResult"

  /cosmetics:
    get:
      operationId: listOwnedCosmetics
      tags: [gacha]
      summary: Get owned cosmetics
      responses:
        "200":
          description: List of owned cosmetics
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/OwnedCosmetic"

  /cosmetics/{id}/equip:
    post:
      operationId: equipCosmetic
      tags: [gacha]
      summary: Equip or unequip a cosmetic
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EquipCosmeticInput"
      responses:
        "200":
          description: Equip status update
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EquipCosmeticResult"

  /me:
    get:
      operationId: getMe
      tags: [users]
      summary: Get current user profile
      responses:
        "200":
          description: Current user
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/User"
        "401":
          description: Unauthorized
    patch:
      operationId: updateMe
      tags: [users]
      summary: Update current user profile
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UserUpdate"
      responses:
        "200":
          description: Updated user
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/User"

  /users:
    get:
      operationId: listUsers
      tags: [users]
      summary: List all users (admin only)
      responses:
        "200":
          description: List of users
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/User"

  /users/switchable:
    get:
      operationId: listSwitchableUsers
      tags: [users]
      summary: List all switchable users
      responses:
        "200":
          description: List of users
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/User"


  /users/{id}/role:
    patch:
      operationId: updateUserRole
      tags: [users]
      summary: Update user role (admin only)
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UserRoleUpdate"
      responses:
        "200":
          description: Updated user
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/User"

  /members:
    get:
      operationId: listMembers
      tags: [users]
      summary: List all members with follow status (authenticated)
      responses:
        "200":
          description: List of members
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/PublicUser"

  /members/{id}:
    get:
      operationId: getPublicProfile
      tags: [users]
      summary: Get public member profile
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Public member profile
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PublicUser"

  /members/{id}/followers:
    get:
      operationId: getPublicProfileFollowers
      tags: [users]
      summary: Get followers of a public member profile
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Public member followers
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/PublicUser"

  /members/{id}/following:
    get:
      operationId: getPublicProfileFollowing
      tags: [users]
      summary: Get accounts followed by a public member profile
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Public member following
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/PublicUser"

  /members/{id}/badges:
    get:
      operationId: getPublicProfileBadges
      tags: [badges]
      summary: Get badges assigned to a public member profile
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Public member badges
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Badge"

  /follows:
    post:
      operationId: followUser
      tags: [follows]
      summary: Follow a user
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/FollowInput"
      responses:
        "201":
          description: Followed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FollowResult"

  /follows/{userId}:
    delete:
      operationId: unfollowUser
      tags: [follows]
      summary: Unfollow a user
      parameters:
        - name: userId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Unfollowed

  /me/following:
    get:
      operationId: getMyFollowing
      tags: [follows]
      summary: Get list of users I follow
      responses:
        "200":
          description: Following list
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/PublicUser"

  /me/followers:
    get:
      operationId: getMyFollowers
      tags: [follows]
      summary: Get list of users following me
      responses:
        "200":
          description: Followers list
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/PublicUser"

  /developments:
    get:
      operationId: listDevelopments
      tags: [developments]
      summary: List all server developments
      responses:
        "200":
          description: List of developments
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Development"
    post:
      operationId: createDevelopment
      tags: [developments]
      summary: Create a development item (admin only)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DevelopmentInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Development"

  /developments/{id}:
    get:
      operationId: getDevelopment
      tags: [developments]
      summary: Get a development item
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Development item
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Development"
    patch:
      operationId: updateDevelopment
      tags: [developments]
      summary: Update a development item (admin only)
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DevelopmentUpdate"
      responses:
        "200":
          description: Updated development
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Development"
    delete:
      operationId: deleteDevelopment
      tags: [developments]
      summary: Delete a development item (admin only)
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Deleted

  /announcements:
    get:
      operationId: listAnnouncements
      tags: [announcements]
      summary: List announcements
      responses:
        "200":
          description: List of announcements
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Announcement"
    post:
      operationId: createAnnouncement
      tags: [announcements]
      summary: Create an announcement (admin only)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AnnouncementInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Announcement"

  /announcements/{id}:
    get:
      operationId: getAnnouncement
      tags: [announcements]
      summary: Get an announcement
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Announcement
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Announcement"
    patch:
      operationId: updateAnnouncement
      tags: [announcements]
      summary: Update an announcement (admin only)
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AnnouncementUpdate"
      responses:
        "200":
          description: Updated announcement
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Announcement"
    delete:
      operationId: deleteAnnouncement
      tags: [announcements]
      summary: Delete an announcement (admin only)
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Deleted

  /admin/follows:
    post:
      operationId: adminCreateFollow
      tags: [follows]
      summary: Force a follow between two users (admin only)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AdminFollowInput"
      responses:
        "201":
          description: Follow created

  /admin/bulk-followers:
    post:
      operationId: adminBulkCreateFollowers
      tags: [follows]
      summary: Generate N bot accounts and auto-follow a target user (admin only)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AdminBulkFollowInput"
      responses:
        "201":
          description: Bulk followers created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminBulkFollowResult"

  /admin/users/{id}:
    patch:
      operationId: adminUpdateUser
      tags: [users]
      summary: Edit any user profile (admin only)
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AdminUserUpdate"
      responses:
        "200":
          description: Updated user
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/User"
    delete:
      operationId: adminDeleteUser
      tags: [users]
      summary: Delete a user profile (admin only)
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: User deleted successfully

  /stats:
    get:
      operationId: getStats
      tags: [stats]
      summary: Get server stats
      responses:
        "200":
          description: Server stats
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServerStats"

  /me/settings:
    get:
      operationId: getMySettings
      tags: [settings]
      summary: Get current user settings
      responses:
        "200":
          description: User settings
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserSettings"
    patch:
      operationId: updateMySettings
      tags: [settings]
      summary: Update current user settings
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UserSettingsUpdate"
      responses:
        "200":
          description: Updated settings
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserSettings"

  /conversations:
    get:
      operationId: listConversations
      tags: [messaging]
      summary: List my conversations
      responses:
        "200":
          description: List of conversations
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ConversationSummary"
    post:
      operationId: createGroup
      tags: [messaging]
      summary: Create a group conversation
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateGroupInput"
      responses:
        "201":
          description: Created group
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConversationSummary"

  /conversations/dm:
    post:
      operationId: createOrGetDm
      tags: [messaging]
      summary: Start or get existing DM conversation
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateDmInput"
      responses:
        "200":
          description: Existing or new DM conversation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConversationSummary"

  /conversations/{id}:
    get:
      operationId: getConversation
      tags: [messaging]
      summary: Get conversation details
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Conversation details
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConversationSummary"
    patch:
      operationId: updateGroup
      tags: [messaging]
      summary: Update group name or icon
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateGroupInput"
      responses:
        "200":
          description: Updated conversation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConversationSummary"
    delete:
      operationId: deleteConversation
      tags: [messaging]
      summary: Delete a conversation
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Deleted

  /conversations/{id}/messages:
    get:
      operationId: listMessages
      tags: [messaging]
      summary: Get messages in a conversation (last 50)
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Messages
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Message"
    post:
      operationId: sendMessage
      tags: [messaging]
      summary: Send a message
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SendMessageInput"
      responses:
        "201":
          description: Sent message
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Message"

  /conversations/{id}/messages/{messageId}:
    delete:
      operationId: deleteMessage
      tags: [messaging]
      summary: Delete a message
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
        - name: messageId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Deleted

  /conversations/{id}/pins:
    get:
      operationId: listPinnedMessages
      tags: [messaging]
      summary: List pinned messages in a conversation
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Pinned messages
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Message"

  /conversations/{id}/messages/{messageId}/pin:
    post:
      operationId: pinMessage
      tags: [messaging]
      summary: Pin a message
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
        - name: messageId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Pinned success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Message"
    delete:
      operationId: unpinMessage
      tags: [messaging]
      summary: Unpin a message
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
        - name: messageId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Unpinned success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Message"

  /conversations/{id}/messages/{messageId}/star:
    post:
      operationId: starMessage
      tags: [messaging]
      summary: Star a message
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
        - name: messageId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Starred success
    delete:
      operationId: unstarMessage
      tags: [messaging]
      summary: Unstar a message
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
        - name: messageId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Unstarred success

  /me/starred:
    get:
      operationId: listStarredMessages
      tags: [messaging]
      summary: List my starred messages
      responses:
        "200":
          description: Starred messages
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Message"

  /conversations/{id}/messages/{messageId}/react:
    post:
      operationId: reactMessage
      tags: [messaging]
      summary: Add an emoji reaction to a message
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
        - name: messageId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReactMessageInput"
      responses:
        "204":
          description: React success

  /conversations/{id}/messages/{messageId}/react/{emoji}:
    delete:
      operationId: unreactMessage
      tags: [messaging]
      summary: Remove an emoji reaction from a message
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
        - name: messageId
          in: path
          required: true
          schema:
            type: integer
        - name: emoji
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Unreact success

  /conversations/{id}/members:
    get:
      operationId: listConversationMembers
      tags: [messaging]
      summary: List members of a conversation
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Members
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ConversationMemberInfo"
    post:
      operationId: addConversationMember
      tags: [messaging]
      summary: Add a member to a group
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AddMemberInput"
      responses:
        "201":
          description: Member added
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConversationMemberInfo"

  /conversations/{id}/members/{userId}:
    delete:
      operationId: removeConversationMember
      tags: [messaging]
      summary: Remove a member from a group
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
        - name: userId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Removed

  /conversations/{id}/invite:
    post:
      operationId: generateInviteCode
      tags: [messaging]
      summary: Generate or retrieve the group invite code
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GenerateInviteCodeInput"
      responses:
        "200":
          description: The group invite code details
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GenerateInviteCodeResult"

  /invites/{code}:
    get:
      operationId: getInviteDetails
      tags: [messaging]
      summary: Get group details using an invite code
      parameters:
        - name: code
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Group details for joining
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InviteDetails"

  /invites/{code}/join:
    post:
      operationId: joinGroupByInviteCode
      tags: [messaging]
      summary: Join a group chat using an invite code
      parameters:
        - name: code
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Joined group details
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/JoinGroupResult"

  /me/friends:
    get:
      operationId: getMyFriends
      tags: [follows]
      summary: Get mutual followers (friends)
      responses:
        "200":
          description: Friends list
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/PublicUser"
  /tickets:
    get:
      operationId: listTickets
      tags: [tickets]
      summary: List support tickets (admins see all, users see their own)
      responses:
        "200":
          description: List of tickets
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Ticket"
    post:
      operationId: createTicket
      tags: [tickets]
      summary: Create a support ticket
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateTicketInput"
      responses:
        "201":
          description: Created ticket
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Ticket"

  /ticket-reasons:
    get:
      operationId: listTicketReasons
      tags: [tickets]
      summary: List active support ticket reasons
      responses:
        "200":
          description: List of active ticket reasons
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/TicketReason"

  /admin/ticket-reasons:
    get:
      operationId: listAdminTicketReasons
      tags: [tickets]
      summary: List all support ticket reasons (admin only)
      responses:
        "200":
          description: List of all ticket reasons
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/TicketReason"
    post:
      operationId: createTicketReason
      tags: [tickets]
      summary: Create a support ticket reason (admin only)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TicketReasonInput"
      responses:
        "201":
          description: Created ticket reason
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TicketReason"

  /admin/ticket-reasons/{id}:
    patch:
      operationId: updateTicketReason
      tags: [tickets]
      summary: Update a support ticket reason (admin only)
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TicketReasonUpdate"
      responses:
        "200":
          description: Updated ticket reason
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TicketReason"
    delete:
      operationId: deleteTicketReason
      tags: [tickets]
      summary: Delete a support ticket reason (admin only)
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Deleted ticket reason

  /tickets/{id}:
    patch:
      operationId: updateTicket
      tags: [tickets]
      summary: Update ticket status or assigned admin
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateTicketInput"
      responses:
        "200":
          description: Updated ticket
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Ticket"

  /tickets/{id}/messages:
    get:
      operationId: listTicketMessages
      tags: [tickets]
      summary: List replies of a ticket
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: List of ticket replies
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/TicketMessage"
    post:
      operationId: sendTicketMessage
      tags: [tickets]
      summary: Send a reply/message inside a ticket
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SendTicketMessageInput"
      responses:
        "201":
          description: Created reply
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TicketMessage"

  /credits:
    get:
      operationId: listCredits
      tags: [credits]
      summary: List all server credits
      responses:
        "200":
          description: List of credits
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Credit"
    post:
      operationId: createCredit
      tags: [credits]
      summary: Create a credit item (admin only)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateCreditInput"
      responses:
        "201":
          description: Created credit item
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Credit"

  /credits/{id}:
    patch:
      operationId: updateCredit
      tags: [credits]
      summary: Update a credit item (admin only)
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateCreditInput"
      responses:
        "200":
          description: Updated credit item
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Credit"
    delete:
      operationId: deleteCredit
      tags: [credits]
      summary: Delete a credit item (admin only)
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Deleted

  /badges:
    get:
      operationId: listBadges
      tags: [badges]
      summary: List all available profile badges
      responses:
        "200":
          description: List of badges
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Badge"

  /admin/users/{id}/badges:
    post:
      operationId: assignUserBadge
      tags: [badges]
      summary: Assign a badge to a user (admin only)
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssignBadgeInput"
      responses:
        "201":
          description: Assigned badge
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Badge"

  /admin/users/{id}/badges/{badgeId}:
    delete:
      operationId: removeUserBadge
      tags: [badges]
      summary: Remove a badge from a user (admin only)
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
        - name: badgeId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Removed badge

  /forms:
    get:
      operationId: listForms
      tags: [forms]
      summary: List forms and polls (admin sees all, members see open only)
      responses:
        "200":
          description: List of forms/polls
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/FormSummary"
    post:
      operationId: createForm
      tags: [forms]
      summary: Admin creates a form or poll
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateFormInput"
      responses:
        "201":
          description: Created form
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FormDetail"

  /forms/{id}:
    get:
      operationId: getForm
      tags: [forms]
      summary: Get form/poll detail including fields or options
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Form detail
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FormDetail"
    patch:
      operationId: updateForm
      tags: [forms]
      summary: Admin updates a form/poll
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateFormInput"
      responses:
        "200":
          description: Updated form
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FormDetail"
    delete:
      operationId: deleteForm
      tags: [forms]
      summary: Admin deletes a form/poll
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Deleted

  /forms/{id}/vote:
    post:
      operationId: submitVote
      tags: [forms]
      summary: Member votes on a poll
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SubmitVoteInput"
      responses:
        "201":
          description: Vote recorded
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FormResponse"

  /forms/{id}/submit:
    post:
      operationId: submitForm
      tags: [forms]
      summary: Member submits a form response
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SubmitFormInput"
      responses:
        "201":
          description: Submission recorded
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FormResponse"

  /forms/{id}/responses:
    get:
      operationId: listFormResponses
      tags: [forms]
      summary: Admin views all responses or vote results for a form/poll
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Form responses or poll results
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FormResponsesResult"

  /forms/{id}/my-response:
    get:
      operationId: getMyFormResponse
      tags: [forms]
      summary: Check if current user has responded to a form/poll
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: User response or null
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MyFormResponse"

  /wallet/transactions:
    get:
      operationId: listWalletTransactions
      tags: [gacha]
      summary: Get current user wallet transactions
      responses:
        "200":
          description: List of transactions
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/WalletTransaction"

  /admin/users/{id}/wallet:
    post:
      operationId: adminAdjustWallet
      tags: [users]
      summary: Adjust a user's wallet balance (admin only)
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AdminAdjustWalletInput"
      responses:
        "200":
          description: Adjust success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminAdjustWalletResult"

  /admin/gacha/settings:
    get:
      operationId: getAdminGachaSettings
      tags: [gacha]
      summary: Get gacha settings (admin only)
      responses:
        "200":
          description: Gacha settings
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GachaSettings"
    post:
      operationId: updateAdminGachaSettings
      tags: [gacha]
      summary: Update gacha settings (admin only)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GachaSettings"
      responses:
        "200":
          description: Settings updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GachaSettings"

  /admin/cosmetics:
    post:
      operationId: adminCreateCosmetic
      tags: [gacha]
      summary: Add a new cosmetic (admin only)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AdminCreateCosmeticInput"
      responses:
        "201":
          description: Cosmetic created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Cosmetic"

  /admin/cosmetics/{id}:
    patch:
      operationId: adminUpdateCosmetic
      tags: [gacha]
      summary: Update a cosmetic (admin only)
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AdminUpdateCosmeticInput"
      responses:
        "200":
          description: Cosmetic updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Cosmetic"
    delete:
      operationId: adminDeleteCosmetic
      tags: [gacha]
      summary: Delete a cosmetic (admin only)
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Cosmetic deleted

components:
  schemas:
    HealthStatus:
      type: object
      properties:
        status:
          type: string
      required:
        - status

    User:
      type: object
      required: [id, clerkId, username, userTag, role, createdAt, diamonds]
      properties:
        id:
          type: integer
        clerkId:
          type: string
        username:
          type: string
        userTag:
          type: string
          pattern: "^#[0-9]{3,}$"
        displayName:
          type: ["string", "null"]
        avatarUrl:
          type: ["string", "null"]
        role:
          type: string
          enum: [member, premium, premium_plus, admin, staff, dev, dev_website, bot, ai]
        bio:
          type: ["string", "null"]
        youtubeLiveUrl:
          type: ["string", "null"]
        mcUsername:
          type: ["string", "null"]
        diamonds:
          type: integer
        equippedBorder:
          type: ["string", "null"]
        equippedBadge:
          type: ["string", "null"]
        equippedBackground:
          type: ["string", "null"]
        createdAt:
          type: string
 
    UserUpdate:
      type: object
      properties:
        displayName:
          type: string
        bio:
          type: string
        youtubeLiveUrl:
          type: string
        username:
          type: string
        mcUsername:
          type: string
 
    UserRoleUpdate:
      type: object
      required: [role]
      properties:
        role:
          type: string
          enum: [member, premium, premium_plus, admin, staff, dev, dev_website, bot, ai]
 
    PublicUser:
      type: object
      required: [id, username, userTag, role, createdAt, isFollowing, followerCount, followingCount]
      properties:
        id:
          type: integer
        username:
          type: string
        userTag:
          type: string
          pattern: "^#[0-9]{3,}$"
        role:
          type: string
          enum: [member, premium, premium_plus, admin, staff, dev, dev_website, bot, ai]
        displayName:
          type: ["string", "null"]
        avatarUrl:
          type: ["string", "null"]
        bio:
          type: ["string", "null"]
        youtubeLiveUrl:
          type: ["string", "null"]
        createdAt:
          type: string
        isFollowing:
          type: boolean
        followerCount:
          type: integer
        followingCount:
          type: integer
        equippedBorder:
          type: ["string", "null"]
        equippedBadge:
          type: ["string", "null"]
        equippedBackground:
          type: ["string", "null"]
 
    FollowInput:
      type: object
      required: [userId]
      properties:
        userId:
          type: integer
 
    AdminBulkFollowInput:
      type: object
      required: [targetUserId, count]
      properties:
        targetUserId:
          type: integer
        count:
          type: integer
          minimum: 1
          maximum: 10000
 
    AdminBulkFollowResult:
      type: object
      required: [botsCreated, followsCreated]
      properties:
        botsCreated:
          type: integer
        followsCreated:
          type: integer
 
    AdminUserUpdate:
      type: object
      properties:
        username:
          type: string
        displayName:
          type: string
        bio:
          type: string
        youtubeLiveUrl:
          type: string
        role:
          type: string
          enum: [member, premium, premium_plus, admin, staff, dev, dev_website, bot, ai]
        mcUsername:
          type: string

    AdminFollowInput:
      type: object
      required: [followerId, followingId]
      properties:
        followerId:
          type: integer
        followingId:
          type: integer

    FollowResult:
      type: object
      required: [followerId, followingId]
      properties:
        followerId:
          type: integer
        followingId:
          type: integer

    Development:
      type: object
      required: [id, title, description, category, status, order, createdAt]
      properties:
        id:
          type: integer
        title:
          type: string
        description:
          type: string
        category:
          type: string
        status:
          type: string
          enum: [planned, in_progress, completed, paused]
        progress:
          type: ["integer", "null"]
        iconName:
          type: ["string", "null"]
        order:
          type: integer
        createdAt:
          type: string
        updatedAt:
          type: string

    DevelopmentInput:
      type: object
      required: [title, description, category, status]
      properties:
        title:
          type: string
          minLength: 1
        description:
          type: string
        category:
          type: string
        status:
          type: string
          enum: [planned, in_progress, completed, paused]
        progress:
          type: integer
        iconName:
          type: string
        order:
          type: integer

    DevelopmentUpdate:
      type: object
      properties:
        title:
          type: string
          minLength: 1
        description:
          type: string
        category:
          type: string
        status:
          type: string
          enum: [planned, in_progress, completed, paused]
        progress:
          type: integer
        iconName:
          type: string
        order:
          type: integer

    Announcement:
      type: object
      required: [id, title, content, type, pinned, createdAt]
      properties:
        id:
          type: integer
        title:
          type: string
        content:
          type: string
        type:
          type: string
          enum: [update, event, maintenance, general]
        pinned:
          type: boolean
        authorId:
          type: ["integer", "null"]
        authorName:
          type: ["string", "null"]
        createdAt:
          type: string
        updatedAt:
          type: string
        imageUrl:
          type: ["string", "null"]

    AnnouncementInput:
      type: object
      required: [title, content, type]
      properties:
        title:
          type: string
          minLength: 1
        content:
          type: string
        type:
          type: string
          enum: [update, event, maintenance, general]
        pinned:
          type: boolean
        imageUrl:
          type: string

    AnnouncementUpdate:
      type: object
      properties:
        title:
          type: string
          minLength: 1
        content:
          type: string
        type:
          type: string
          enum: [update, event, maintenance, general]
        pinned:
          type: boolean
        imageUrl:
          type: string

    ServerStats:
      type: object
      required: [totalMembers, totalAnnouncements, totalDevelopments, completedDevelopments, inProgressDevelopments]
      properties:
        totalMembers:
          type: integer
        totalAnnouncements:
          type: integer
        totalDevelopments:
          type: integer
        completedDevelopments:
          type: integer
        inProgressDevelopments:
          type: integer

    UserSettings:
      type: object
      required: [messagePrivacy]
      properties:
        messagePrivacy:
          type: string
          enum: [everyone, friends_only, following_only, nobody]

    UserSettingsUpdate:
      type: object
      properties:
        messagePrivacy:
          type: string
          enum: [everyone, friends_only, following_only, nobody]

    ConversationSummary:
      type: object
      required: [id, type, createdAt, memberCount]
      properties:
        id:
          type: integer
        type:
          type: string
          enum: [dm, group]
        name:
          type: ["string", "null"]
        iconUrl:
          type: ["string", "null"]
        bannerUrl:
          type: ["string", "null"]
        description:
          type: ["string", "null"]
        ownerId:
          type: ["integer", "null"]
        memberCount:
          type: integer
        otherUserId:
          type: ["integer", "null"]
        otherUsername:
          type: ["string", "null"]
        otherDisplayName:
          type: ["string", "null"]
        otherAvatarUrl:
          type: ["string", "null"]
        otherUserRole:
          type: ["string", "null"]
        otherUserEquippedBorder:
          type: ["string", "null"]
        lastMessageContent:
          type: ["string", "null"]
        lastMessageAt:
          type: ["string", "null"]
        lastMessageSenderId:
          type: ["integer", "null"]
        createdAt:
          type: string
        inviteCode:
          type: ["string", "null"]

    Message:
      type: object
      required: [id, conversationId, content, createdAt]
      properties:
        id:
          type: integer
        conversationId:
          type: integer
        senderId:
          type: ["integer", "null"]
        title:
          type: ["string", "null"]
        content:
          type: string
        imageUrl:
          type: ["string", "null"]
        attachmentDriveFileId:
          type: ["string", "null"]
        attachmentUrl:
          type: ["string", "null"]
        attachmentName:
          type: ["string", "null"]
        attachmentMime:
          type: ["string", "null"]
        attachmentSize:
          type: ["integer", "null"]
        forwardedFromMessageId:
          type: ["integer", "null"]
        forwardedFromConversationId:
          type: ["integer", "null"]
        replyToMessageId:
          type: ["integer", "null"]
        replyToMessageContent:
          type: ["string", "null"]
        replyToMessageSenderUsername:
          type: ["string", "null"]
        pinned:
          type: boolean
        pinnedAt:
          type: ["string", "null"]
          format: date-time
        pinnedByUserId:
          type: ["integer", "null"]
        deletedAt:
          type: ["string", "null"]
          format: date-time
        deletedByUserId:
          type: ["integer", "null"]
        deletedScope:
          type: ["string", "null"]
        createdAt:
          type: string
        updatedAt:
          type: ["string", "null"]
          format: date-time
        senderUsername:
          type: ["string", "null"]
        senderDisplayName:
          type: ["string", "null"]
        senderAvatarUrl:
          type: ["string", "null"]
        senderRole:
          type: ["string", "null"]
        senderEquippedBorder:
          type: ["string", "null"]
        starred:
          type: boolean
        reactions:
          type: array
          items:
            $ref: "#/components/schemas/MessageReactionSummary"

    MessageReactionSummary:
      type: object
      required: [emoji, count, userReacted]
      properties:
        emoji:
          type: string
        count:
          type: integer
        userReacted:
          type: boolean
        usernames:
          type: array
          items:
            type: string

    ConversationMemberInfo:
      type: object
      required: [userId, username, userTag, mentionTag, joinedAt]
      properties:
        id:
          type: integer
        userId:
          type: integer
        username:
          type: string
        userTag:
          type: string
        mentionTag:
          type: string
        displayName:
          type: ["string", "null"]
        avatarUrl:
          type: ["string", "null"]
        role:
          type: ["string", "null"]
        joinedAt:
          type: string
        roles:
          type: array
          items:
            type: object
            required: [id, name, color]
            properties:
              id:
                type: integer
              name:
                type: string
              color:
                type: string

    CreateDmInput:
      type: object
      required: [targetUserId]
      properties:
        targetUserId:
          type: integer

    GenerateInviteCodeInput:
      type: object
      properties:
        regenerate:
          type: boolean

    GenerateInviteCodeResult:
      type: object
      required: [inviteCode]
      properties:
        inviteCode:
          type: ["string", "null"]

    InviteDetails:
      type: object
      required: [id, name, memberCount]
      properties:
        id:
          type: integer
        name:
          type: string
        iconUrl:
          type: ["string", "null"]
        memberCount:
          type: integer

    JoinGroupResult:
      type: object
      required: [conversationId]
      properties:
        conversationId:
          type: integer

    CreateGroupInput:
      type: object
      required: [name, memberIds]
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 100
        memberIds:
          type: array
          items:
            type: integer

    UpdateGroupInput:
      type: object
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 100
        description:
          type: ["string", "null"]
          maxLength: 500
        iconUrl:
          type: ["string", "null"]
        bannerUrl:
          type: ["string", "null"]

    SendMessageInput:
      type: object
      properties:
        title:
          type: string
        content:
          type: string
          minLength: 1
          maxLength: 4000
        imageUrl:
          type: string
        attachmentDriveFileId:
          type: string
        attachmentUrl:
          type: string
        attachmentName:
          type: string
        attachmentMime:
          type: string
        attachmentSize:
          type: integer
        replyToMessageId:
          type: integer

    ReactMessageInput:
      type: object
      required: [emoji]
      properties:
        emoji:
          type: string

    AddMemberInput:
      type: object
      required: [userId]
      properties:
        userId:
          type: integer

    Ticket:
      type: object
      required: [id, creatorId, reason, description, status, createdAt, updatedAt]
      properties:
        id:
          type: integer
        creatorId:
          type: integer
        reason:
          type: string
        description:
          type: string
        status:
          type: string
          enum: [open, in_progress, resolved, closed]
        adminId:
          type: ["integer", "null"]
        createdAt:
          type: string
        updatedAt:
          type: string
        creatorUsername:
          type: ["string", "null"]
        creatorDisplayName:
          type: ["string", "null"]
        adminUsername:
          type: ["string", "null"]
        adminDisplayName:
          type: ["string", "null"]

    CreateTicketInput:
      type: object
      required: [reason, description]
      properties:
        reason:
          type: string
        description:
          type: string
          minLength: 5
          maxLength: 2000

    TicketReason:
      type: object
      required: [id, label, isActive, order, createdAt, updatedAt]
      properties:
        id:
          type: integer
        label:
          type: string
        description:
          type: ["string", "null"]
        isActive:
          type: boolean
        order:
          type: integer
        createdAt:
          type: string
        updatedAt:
          type: string

    TicketReasonInput:
      type: object
      required: [label]
      properties:
        label:
          type: string
          minLength: 1
          maxLength: 120
        description:
          type: string
          maxLength: 500
        isActive:
          type: boolean
        order:
          type: integer

    TicketReasonUpdate:
      type: object
      properties:
        label:
          type: string
          minLength: 1
          maxLength: 120
        description:
          type: string
          maxLength: 500
        isActive:
          type: boolean
        order:
          type: integer

    UpdateTicketInput:
      type: object
      properties:
        status:
          type: string
          enum: [open, in_progress, resolved, closed]
        adminId:
          type: ["integer", "null"]

    TicketMessage:
      type: object
      required: [id, ticketId, senderId, content, createdAt, senderUsername]
      properties:
        id:
          type: integer
        ticketId:
          type: integer
        senderId:
          type: integer
        content:
          type: string
        createdAt:
          type: string
        senderUsername:
          type: string
        senderDisplayName:
          type: ["string", "null"]
        senderAvatarUrl:
          type: ["string", "null"]

    SendTicketMessageInput:
      type: object
      required: [content]
      properties:
        content:
          type: string
          minLength: 1
          maxLength: 1000

    FormField:
      type: object
      required: [id, formId, label, fieldType, required, order]
      properties:
        id:
          type: integer
        formId:
          type: integer
        label:
          type: string
        fieldType:
          type: string
          enum: [text, textarea, radio, checkbox, select]
        options:
          type: ["string", "null"]
        required:
          type: boolean
        order:
          type: integer

    PollOption:
      type: object
      required: [id, formId, label, order]
      properties:
        id:
          type: integer
        formId:
          type: integer
        label:
          type: string
        order:
          type: integer
        voteCount:
          type: integer

    FormSummary:
      type: object
      required: [id, title, type, status, createdAt]
      properties:
        id:
          type: integer
        title:
          type: string
        description:
          type: ["string", "null"]
        type:
          type: string
          enum: [form, poll]
        status:
          type: string
          enum: [open, closed]
        createdBy:
          type: integer
        createdByUsername:
          type: ["string", "null"]
        deadline:
          type: ["string", "null"]
        responseCount:
          type: integer
        createdAt:
          type: string
        updatedAt:
          type: string

    FormDetail:
      type: object
      required: [id, title, type, status, createdAt]
      properties:
        id:
          type: integer
        title:
          type: string
        description:
          type: ["string", "null"]
        type:
          type: string
          enum: [form, poll]
        status:
          type: string
          enum: [open, closed]
        createdBy:
          type: integer
        createdByUsername:
          type: ["string", "null"]
        deadline:
          type: ["string", "null"]
        responseCount:
          type: integer
        createdAt:
          type: string
        updatedAt:
          type: string
        fields:
          type: array
          items:
            $ref: "#/components/schemas/FormField"
        options:
          type: array
          items:
            $ref: "#/components/schemas/PollOption"

    CreateFormInput:
      type: object
      required: [title, type]
      properties:
        title:
          type: string
          minLength: 1
          maxLength: 255
        description:
          type: string
        type:
          type: string
          enum: [form, poll]
        deadline:
          type: string
        fields:
          type: array
          items:
            type: object
            required: [label, fieldType]
            properties:
              label:
                type: string
              fieldType:
                type: string
                enum: [text, textarea, radio, checkbox, select]
              options:
                type: string
              required:
                type: boolean
              order:
                type: integer
        options:
          type: array
          items:
            type: object
            required: [label]
            properties:
              label:
                type: string
              order:
                type: integer

    UpdateFormInput:
      type: object
      properties:
        title:
          type: string
          minLength: 1
          maxLength: 255
        description:
          type: ["string", "null"]
        status:
          type: string
          enum: [open, closed]
        deadline:
          type: string
        fields:
          type: array
          items:
            type: object
            required: [label, fieldType]
            properties:
              id:
                type: integer
              label:
                type: string
              fieldType:
                type: string
                enum: [text, textarea, radio, checkbox, select]
              options:
                type: string
              required:
                type: boolean
              order:
                type: integer
        options:
          type: array
          items:
            type: object
            required: [label]
            properties:
              id:
                type: integer
              label:
                type: string
              order:
                type: integer

    SubmitVoteInput:
      type: object
      required: [optionId]
      properties:
        optionId:
          type: integer

    SubmitFormInput:
      type: object
      required: [answers]
      properties:
        answers:
          type: array
          items:
            type: object
            required: [fieldId, value]
            properties:
              fieldId:
                type: integer
              value:
                type: string

    FormResponse:
      type: object
      required: [id, formId, userId, createdAt]
      properties:
        id:
          type: integer
        formId:
          type: integer
        userId:
          type: integer
        selectedOptionId:
          type: ["integer", "null"]
        createdAt:
          type: string

    FormResponseDetail:
      type: object
      required: [id, userId, username, createdAt]
      properties:
        id:
          type: integer
        userId:
          type: integer
        username:
          type: string
        displayName:
          type: ["string", "null"]
        mcUsername:
          type: ["string", "null"]
        selectedOptionId:
          type: ["integer", "null"]
        selectedOptionLabel:
          type: ["string", "null"]
        createdAt:
          type: string
        answers:
          type: array
          items:
            type: object
            required: [fieldId, fieldLabel, value]
            properties:
              fieldId:
                type: integer
              fieldLabel:
                type: string
              value:
                type: string

    FormResponsesResult:
      type: object
      required: [formId, type, responses]
      properties:
        formId:
          type: integer
        type:
          type: string
          enum: [form, poll]
        responses:
          type: array
          items:
            $ref: "#/components/schemas/FormResponseDetail"
        pollResults:
          type: array
          items:
            type: object
            required: [optionId, label, count]
            properties:
              optionId:
                type: integer
              label:
                type: string
              count:
                type: integer

    MyFormResponse:
      type: object
      properties:
        hasResponded:
          type: boolean
        response:
          $ref: "#/components/schemas/FormResponseDetail"

    Credit:
      type: object
      required: [id, name, role, borderType, order, createdAt, updatedAt]
      properties:
        id:
          type: integer
        name:
          type: string
        avatarUrl:
          type: ["string", "null"]
        backgroundUrl:
          type: ["string", "null"]
        role:
          type: string
        description:
          type: ["string", "null"]
        borderType:
          type: string
        order:
          type: integer
        createdAt:
          type: string
        updatedAt:
          type: string

    Badge:
      type: object
      required: [id, key, label, color, order, createdAt]
      properties:
        id:
          type: integer
        key:
          type: string
        label:
          type: string
        color:
          type: string
        description:
          type: ["string", "null"]
        order:
          type: integer
        createdAt:
          type: string

    AssignBadgeInput:
      type: object
      required: [badgeId]
      properties:
        badgeId:
          type: integer

    CreateCreditInput:
      type: object
      required: [name, role]
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 255
        avatarUrl:
          type: string
        backgroundUrl:
          type: string
        role:
          type: string
          minLength: 1
          maxLength: 255
        description:
          type: string
        borderType:
          type: string
          default: "frame1"
        order:
          type: integer

    UpdateCreditInput:
      type: object
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 255
        avatarUrl:
          type: string
        backgroundUrl:
          type: string
        role:
          type: string
          minLength: 1
          maxLength: 255
        description:
          type: string
        borderType:
          type: string
        order:
          type: integer

    Cosmetic:
      type: object
      required: [id, name, type, rarity, value, createdAt]
      properties:
        id:
          type: integer
        name:
          type: string
        type:
          type: string
          enum: [badge, border, background]
        rarity:
          type: string
          enum: [S, A, B, C, D]
        value:
          type: string
        description:
          type: ["string", "null"]
        createdAt:
          type: string

    OwnedCosmetic:
      type: object
      required: [id, name, type, rarity, value, isEquipped]
      properties:
        id:
          type: integer
        name:
          type: string
        type:
          type: string
          enum: [badge, border, background]
        rarity:
          type: string
          enum: [S, A, B, C, D]
        value:
          type: string
        description:
          type: ["string", "null"]
        isEquipped:
          type: boolean

    GachaBoardResult:
      type: object
      required: [diamonds, cosmetics, ownedCosmeticIds]
      properties:
        diamonds:
          type: integer
        cosmetics:
          type: array
          items:
            $ref: "#/components/schemas/Cosmetic"
        ownedCosmeticIds:
          type: array
          items:
            type: integer

    GachaClaimResult:
      type: object
      required: [diamonds]
      properties:
        diamonds:
          type: integer

    SpinGachaInput:
      type: object
      required: [count]
      properties:
        count:
          type: integer
          enum: [1, 10, 25, 50]

    SpinGachaResultItem:
      type: object
      required: [cosmetic, isDuplicate]
      properties:
        cosmetic:
          $ref: "#/components/schemas/Cosmetic"
        isDuplicate:
          type: boolean
        refundAmount:
          type: integer

    SpinGachaResult:
      type: object
      required: [results, diamonds, cost, refunded]
      properties:
        results:
          type: array
          items:
            $ref: "#/components/schemas/SpinGachaResultItem"
        diamonds:
          type: integer
        cost:
          type: integer
        refunded:
          type: integer

    EquipCosmeticInput:
      type: object
      properties:
        equip:
          type: boolean

    EquipCosmeticResult:
      type: object
      required: [success, cosmeticId, isEquipped]
      properties:
        success:
          type: boolean
        cosmeticId:
          type: integer
        isEquipped:
          type: boolean

    WalletTransaction:
      type: object
      required: [id, userId, amount, type, createdAt]
      properties:
        id:
          type: integer
        userId:
          type: integer
        amount:
          type: integer
        type:
          type: string
        description:
          type: ["string", "null"]
        createdAt:
          type: string

    AdminAdjustWalletInput:
      type: object
      required: [amount]
      properties:
        amount:
          type: integer
        reason:
          type: string

    AdminAdjustWalletResult:
      type: object
      required: [diamonds]
      properties:
        diamonds:
          type: integer

    GachaSettings:
      type: object
      required: [spinCost1, spinCost10, spinCost25, spinCost50, duplicateRefund, rateS, rateA, rateB, rateC]
      properties:
        spinCost1:
          type: integer
        spinCost10:
          type: integer
        spinCost25:
          type: integer
        spinCost50:
          type: integer
        duplicateRefund:
          type: integer
        rateS:
          type: number
        rateA:
          type: number
        rateB:
          type: number
        rateC:
          type: number

    AdminCreateCosmeticInput:
      type: object
      required: [name, type, rarity, value]
      properties:
        name:
          type: string
          minLength: 1
        type:
          type: string
          enum: [badge, border, background]
        rarity:
          type: string
          enum: [S, A, B, C, D]
        value:
          type: string
          minLength: 1
        description:
          type: string

    AdminUpdateCosmeticInput:
      type: object
      properties:
        name:
          type: string
        type:
          type: string
          enum: [badge, border, background]
        rarity:
          type: string
          enum: [S, A, B, C, D]
        value:
          type: string
        description:
          type: string
